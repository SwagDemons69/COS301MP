import { apiSearchModalUtil } from './api-search-modal-util';

describe('apiSearchModalUtil', () => {
  it('should work', () => {
    expect(apiSearchModalUtil()).toEqual('api-search-modal-util');
  });
});
