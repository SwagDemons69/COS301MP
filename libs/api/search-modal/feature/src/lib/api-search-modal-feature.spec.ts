import { apiSearchModalFeature } from './api-search-modal-feature';

describe('apiSearchModalFeature', () => {
  it('should work', () => {
    expect(apiSearchModalFeature()).toEqual('api-search-modal-feature');
  });
});
