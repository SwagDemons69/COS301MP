import { apiSearchModalDataAccess } from './api-search-modal-data-access';

describe('apiSearchModalDataAccess', () => {
  it('should work', () => {
    expect(apiSearchModalDataAccess()).toEqual('api-search-modal-data-access');
  });
});
