import { apiCommentFeature } from './api-comment-feature';

describe('apiCommentFeature', () => {
  it('should work', () => {
    expect(apiCommentFeature()).toEqual('api-comment-feature');
  });
});
