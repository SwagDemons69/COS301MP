import { apiLikeDataAccess } from './api-like-data-access';

describe('apiLikeDataAccess', () => {
  it('should work', () => {
    expect(apiLikeDataAccess()).toEqual('api-like-data-access');
  });
});
