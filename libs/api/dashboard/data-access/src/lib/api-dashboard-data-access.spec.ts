import { apiDashboardDataAccess } from './api-dashboard-data-access';

describe('apiDashboardDataAccess', () => {
  it('should work', () => {
    expect(apiDashboardDataAccess()).toEqual('api-dashboard-data-access');
  });
});
