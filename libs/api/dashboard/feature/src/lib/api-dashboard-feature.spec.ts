import { apiDashboardFeature } from './api-dashboard-feature';

describe('apiDashboardFeature', () => {
  it('should work', () => {
    expect(apiDashboardFeature()).toEqual('api-dashboard-feature');
  });
});
