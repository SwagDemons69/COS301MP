import { appKronosTimerKronos } from './app-kronos-timer-kronos';

describe('appKronosTimerKronos', () => {
  it('should work', () => {
    expect(appKronosTimerKronos()).toEqual('app-kronos-timer-kronos');
  });
});
