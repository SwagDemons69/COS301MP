import { appSearchModalUtil } from './app-search-modal-util';

describe('appSearchModalUtil', () => {
  it('should work', () => {
    expect(appSearchModalUtil()).toEqual('app-search-modal-util');
  });
});
