import { appSearchModalFeature } from './app-search-modal-feature';

describe('appSearchModalFeature', () => {
  it('should work', () => {
    expect(appSearchModalFeature()).toEqual('app-search-modal-feature');
  });
});
