import { appSearchModalDataAccess } from './app-search-modal-data-access';

describe('appSearchModalDataAccess', () => {
  it('should work', () => {
    expect(appSearchModalDataAccess()).toEqual('app-search-modal-data-access');
  });
});
